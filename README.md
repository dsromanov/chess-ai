# Chess AI - Шахматный движок с искусственным интеллектом

Шахматный движок на C++ с красивым графическим интерфейсом на Qt и мощным AI на основе алгоритма Minimax с Alpha-Beta отсечением.



## 🎯 Особенности

### Игровой движок
- ✅ Полная реализация шахматных правил
- ✅ Поддержка всех специальных ходов (рокировка, взятие на проходе, превращение пешки)
- ✅ Детекция шаха, мата и пата
- ✅ Правило 50 ходов
- ✅ Поддержка FEN нотации

### AI Движок
- 🤖 Алгоритм Minimax с Alpha-Beta отсечением
- 🎯 Quiescence search для стабильной оценки
- 📊 Сложная оценочная функция:
  - Материальная оценка
  - Piece-Square Tables
  - Оценка мобильности
  - Безопасность короля
  - Структура пешек
- ⚡ Упорядочивание ходов для оптимального отсечения
- 🎚️ Регулируемая сложность (глубина 2-8 полуходов)

### Графический интерфейс
- 🎨 Красивая шахматная доска с Unicode символами фигур
- 🖱️ Перетаскивание фигур мышью
- ✨ Подсветка легальных ходов
- 🔄 Переворот доски
- 📜 История ходов
- ⏪ Отмена хода
- 📊 Статус игры в реальном времени

## 🛠️ Технологии

- **Язык**: C++17
- **GUI фреймворк**: Qt 5
- **Система сборки**: CMake 3.16+
- **Архитектура**: Model-View разделение

## 📦 Структура проекта

```
chess-ai/
├── include/
│   ├── core/          # Ядро шахматного движка
│   │   ├── Types.h           # Базовые типы
│   │   ├── Piece.h           # Класс фигуры
│   │   ├── Move.h            # Класс хода
│   │   ├── Position.h        # Позиция (состояние игры)
│   │   ├── Board.h           # Шахматная доска
│   │   ├── MoveGenerator.h   # Генератор ходов
│   │   └── MoveValidator.h   # Валидатор ходов
│   ├── ai/            # AI движок
│   │   ├── Engine.h          # AI движок (Minimax)
│   │   └── Evaluator.h       # Оценочная функция
│   └── ui/            # Графический интерфейс
│       ├── ChessBoard.h      # Виджет доски
│       ├── PieceWidget.h     # Виджет фигуры
│       └── MainWindow.h      # Главное окно
├── src/               # Реализация
├── resources/         # Ресурсы Qt
├── CMakeLists.txt     # Конфигурация CMake
└── README.md
```

## 🚀 Сборка и запуск

### Требования

- CMake 3.16 или выше
- C++17 совместимый компилятор (GCC 7+, Clang 5+, MSVC 2017+)
- Qt 5

### Ubuntu/Debian

```bash
# Установка зависимостей
sudo apt update
sudo apt install build-essential cmake qt6-base-dev

# Клонирование репозитория
git clone https://github.com/yourusername/chess-ai.git
cd chess-ai

# Сборка
mkdir build && cd build
cmake ..
make -j$(nproc)

# Запуск
./chess-ai
```

### macOS

```bash
# Установка зависимостей
brew install cmake qt6

# Сборка
mkdir build && cd build
cmake ..
make -j$(sysctl -n hw.ncpu)

# Запуск
./chess-ai
```

### Windows

```bash
# Установить Qt с официального сайта
# Использовать Qt Creator или:

mkdir build && cd build
cmake -G "Visual Studio 16 2019" ..
cmake --build . --config Release

# Запуск
Release\chess-ai.exe
```

## 🎮 Использование

1. **Начать новую игру**: Нажмите "Новая игра" или `Игра > Новая игра`
2. **Сделать ход**: Перетащите фигуру мышью на нужную клетку
3. **Подсказка легальных ходов**: Нажмите на фигуру, чтобы увидеть возможные ходы
4. **Изменить сложность**: Выберите уровень в выпадающем списке "Сложность AI"
5. **Отменить ход**: Нажмите кнопку "Отменить ход"
6. **Перевернуть доску**: Нажмите "Перевернуть доску"

## 🧠 Как работает AI

### Minimax с Alpha-Beta

AI использует классический алгоритм Minimax с Alpha-Beta отсечением:

```cpp
int alphaBeta(depth, alpha, beta, color) {
    if (depth == 0) return quiescence(alpha, beta, color);
    
    for (move : generateMoves()) {
        makeMove(move);
        score = -alphaBeta(depth-1, -beta, -alpha, oppositeColor);
        unmakeMove(move);
        
        if (score >= beta) return beta;  // Beta cutoff
        if (score > alpha) alpha = score;
    }
    return alpha;
}
```

### Оценочная функция

Позиция оценивается по следующим критериям:
- **Материал**: Ценность фигур (пешка=100, конь/слон=300, ладья=500, ферзь=900)
- **Позиция**: Piece-Square Tables для оценки расположения фигур
- **Мобильность**: Количество доступных ходов
- **Безопасность короля**: Пешечный щит, открытые линии
- **Структура пешек**: Сдвоенные, изолированные пешки

## 📈 Производительность

- **Глубина 4**: ~1000-5000 узлов, ~0.1-0.5 сек
- **Глубина 6**: ~10000-50000 узлов, ~1-3 сек
- **Глубина 8**: ~100000-500000 узлов, ~10-30 сек

(Производительность зависит от позиции и CPU)


## 🔮 Будущие улучшения

- [ ] **Сохранение/загрузка игр** (PGN формат)
- [ ] **Онлайн режим** (игра по сети)
- [ ] **Открытая библиотека** (дебюты)
- [ ] **Эндшпильные таблицы**
- [ ] **Мультиплеер** (локальный и онлайн)
- [ ] **Анализ позиции**
- [ ] **Темы оформления**
- [ ] **Звуковые эффекты**
- [ ] **3D режим**
- [ ] **Bitboards** для оптимизации


# chess-ai
# chess-ai
